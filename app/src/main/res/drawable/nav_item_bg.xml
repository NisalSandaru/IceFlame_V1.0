<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- selected state -->
    <item android:drawable="@drawable/circle_bg" android:state_checked="true"/>
    <!-- default state -->
    <item android:drawable="@android:color/transparent"/>
</selector>